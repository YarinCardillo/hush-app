---
status: complete
phase: E+2.5-critical-integration-fixes
source: E+2.5-01-SUMMARY.md, E+2.5-02-SUMMARY.md
started: 2026-03-04T12:00:00Z
updated: 2026-03-05T18:15:00Z
---

## Current Test
<!-- OVERWRITE each test - shows where we are -->

[testing complete]

## Tests

### 1. Message History Loads in Text Channel
expected: Open a server and click on a text channel. Messages should load correctly — the chat area shows existing message history from that channel. No errors in console about missing API paths or 404s.
result: issue
reported: "cant create servers neither with instance owner (mobile ios safari) — modals rendered inside mobile drawer were clipped by overflow:hidden on iOS Safari"
severity: blocker
fix: portaled all modals to document.body via createPortal (commit 58b120a)

### 2. Kick/Ban Force-Disconnect + Toast
expected: When a user is kicked or banned from a server, they should be immediately disconnected (WebSocket closed). Instead of being logged out, they see a toast notification ("You were removed from [server]" or "You were banned from [server]") and are redirected to another server or the /guilds page after ~2.5 seconds.
result: issue
reported: "on macos i cant see the ban taking effect immediately (no broadcast?) and when refreshing, he gets logged out but instead of going to home it goes to a server page with 'guild' placeholder"
severity: major

### 3. Ban/Mute List Modal (Admin View)
expected: As an admin or owner, click the gear icon in the ChannelList sidebar header. A modal opens with two tabs: "Banned Users" and "Muted Users", each showing a count. The lists display active bans/mutes with user ID, reason, date, and expiry info.
result: pass

### 4. Unban User from Modal
expected: In the Ban/Mute List Modal on the "Banned Users" tab, click the Unban button on a banned user. An inline reason input appears. Enter a reason and confirm. The user is removed from the list, and a success toast appears.
result: pass

### 5. Unmute User from Modal
expected: In the Ban/Mute List Modal on the "Muted Users" tab, click the Unmute button on a muted user. An inline reason input appears. Enter a reason and confirm. The user is removed from the list, and a success toast appears.
result: pass

### 6. Gear Icon Visibility (Admin Only)
expected: The gear icon in the ChannelList header is only visible when the current user has admin or owner role. Regular members should NOT see the gear icon.
result: pass

## Summary

total: 6
passed: 4
issues: 2
pending: 0
skipped: 0

## Gaps

- truth: "Modals (Create Server, Create Channel, etc.) should open and be interactive on mobile iOS Safari"
  status: fixed
  reason: "User reported: cant create servers on mobile iOS Safari — modals clipped by overflow:hidden drawer"
  severity: blocker
  test: 1
  root_cause: "Modals rendered inline inside .sidebar-panel-left drawer with overflow:hidden. iOS Safari clips position:fixed descendants."
  fix: "Added createPortal(…, document.body) to GuildCreateModal, ConfirmModal, CreateChannelModal, CreateCategoryModal, InviteModal, UserSettingsModal, ServerSettingsModal (commit 58b120a)"

- truth: "Banned user is immediately force-disconnected via WS and sees toast, then redirected to /guilds or home"
  status: failed
  reason: "User reported: ban doesn't take effect immediately (no broadcast), and after refresh banned user sees server page with 'guild' placeholder instead of home"
  severity: major
  test: 2
  root_cause: ""
  artifacts: []
  missing: []
